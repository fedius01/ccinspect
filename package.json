{
  "name": "ccinspect",
  "version": "0.1.0",
  "description": "Claude Code Configuration Inspector — analyze, lint, and resolve Claude Code configs",
  "type": "module",
  "bin": {
    "ccinspect": "./dist/cli/index.js"
  },
  "scripts": {
    "dev": "tsx src/cli/index.ts",
    "build": "tsup",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/ tests/ && tsc --noEmit",
    "format": "prettier --write 'src/**/*.ts' 'tests/**/*.ts'"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": ["claude", "claude-code", "configuration", "linter", "inspector"],
  "license": "MIT",
  "dependencies": {
    "chalk": "^5.4.1",
    "commander": "^13.1.0",
    "fast-glob": "^3.3.3",
    "gray-matter": "^4.0.3",
    "ora": "^8.2.0",
    "tiktoken": "^1.0.21"
  },
  "devDependencies": {
    "@types/node": "^22.12.0",
    "eslint": "^9.20.0",
    "prettier": "^3.5.0",
    "tsup": "^8.4.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
