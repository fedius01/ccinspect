---
paths:
  - "src/**/*.ts"
  - "src/**/*.tsx"
description: TypeScript coding standards
---

# TypeScript Rules

- Use explicit return types on all exported functions
- Prefer `interface` over `type` for object shapes
- Use `readonly` modifier on immutable properties
